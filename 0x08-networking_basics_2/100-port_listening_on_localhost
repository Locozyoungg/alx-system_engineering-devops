#!/usr/bin/env bash

# Bash script to listen on port 98 on localhost

# Check if the script is run as root
if [[ $EUID -ne 0 ]]; then
    echo "Please run as root (sudo) to listen on privileged ports."
    exit 1
fi

# Use netcat to listen on port 98
nc -l -p 98 -k

